<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chapter05.mapper.Chapter05DeptMapper">
    <resultMap id="deptWithEmpsMap" type="com.example.chapter05.entity.Dept">
        <id property="deptno" column="deptno"/>
        <result property="dname" column="dname"/>
        <result property="loc" column="loc"/>
        <collection property="emps" ofType="com.example.chapter05.entity.Emp">
            <id property="empno" column="empno"/>
            <result property="ename" column="ename"/>
            <result property="job" column="job"/>
            <result property="mgr" column="mgr"/>
            <result property="hiredate" column="hiredate"/>
            <result property="sal" column="sal"/>
            <result property="comm" column="comm"/>
            <result property="deptno" column="emp_deptno"/>
        </collection>
    </resultMap>

    <select id="one2manyByXml" parameterType="java.lang.Integer" resultMap="deptWithEmpsMap">
        SELECT d.deptno, d.dname, d.loc,
               e.empno, e.ename, e.job, e.mgr, e.hiredate, e.sal, e.comm,
               e.deptno AS emp_deptno
        FROM dept d LEFT JOIN emp e ON d.deptno = e.deptno
        WHERE d.deptno = #{deptno}
        ORDER BY e.empno
    </select>
</mapper>
